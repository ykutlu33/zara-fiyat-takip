name: Zara Fiyat Takip

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-price:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests playwright beautifulsoup4
          playwright install firefox
          playwright install-deps

      - name: Check price
        run: |
          python << 'PYEND'
          import requests
          import os
          import time
          import json
          import random
          from datetime import datetime
          from bs4 import BeautifulSoup
          from playwright.sync_api import sync_playwright

          TOKEN = "8375130122:AAHXVaMo-XXZvkd-MEsoOdEVpdtkqc50psg"
          
          GROUP_CHAT = "-5170519870"
          PERSONAL_CHAT = "1234645010"
          
          URL = "https://www.zara.com/tr/tr/cift-tarafli-cift-yuzlu-ceket-p06318254.html"

          def send_telegram(msg, chat_id=GROUP_CHAT):
              url = f"https://api.telegram.org/bot{TOKEN}/sendMessage"
              payload = {"chat_id": chat_id, "text": msg, "parse_mode": "HTML", "disable_web_page_preview": True}
              try:
                  r = requests.post(url, json=payload)
                  return r.status_code == 200
              except:
                  return False

          def send_to_group(msg):
              send_telegram(msg, GROUP_CHAT)

          def send_to_admin(msg):
              send_telegram(msg, PERSONAL_CHAT)

          def get_data():
              if os.path.exists("data.json"):
                  try:
                      return json.load(open("data.json"))
                  except:
                      pass
              return {"price": 0, "first_price": 0, "lowest_price": 0, "highest_price": 0, "check_count": 0, "start_date": ""}

          def save_data(data):
              with open("data.json", "w") as f:
                  json.dump(data, f)

          def format_price(p):
              return f"{p:,.2f} TL".replace(",", "X").replace(".", ",").replace("X", ".")

          def get_time():
              return datetime.now().strftime("%d.%m.%Y %H:%M")

          price = None
          product_name = "Zara Urunu"

          try:
              with sync_playwright() as p:
                  browser = p.firefox.launch(headless=True)
                  context = browser.new_context(
                      user_agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:109.0) Gecko/20100101 Firefox/115.0"
                  )
                  page = context.new_page()
                  page.goto(URL, timeout=90000, wait_until="domcontentloaded")
                  time.sleep(random.uniform(5, 8))
                  content = page.content()
                  browser.close()

                  soup = BeautifulSoup(content, "html.parser")

                  scripts = soup.find_all("script", type="application/ld+json")
                  for script in scripts:
                      try:
                          data = json.loads(script.string)
                          if isinstance(data, list): data = data[0]
                          if data.get("@type") == "Product":
                              if "name" in data: product_name = data["name"]
                              offers = data.get("offers", {})
                              if isinstance(offers, list): offers = offers[0]
                              if offers.get("price"):
                                  price = float(offers["price"])
                                  break
                      except:
                          continue
                  
                  if not price:
                      candidates = soup.select("span.money-amount__main")
                      for tag in candidates:
                          txt = tag.get_text().strip()
                          clean = txt.replace("TL", "").replace("TRY", "").replace(".", "").replace(",", ".").strip()
                          try:
                              val = float(clean)
                              if val > 100:
                                  price = val
                                  break
                          except:
                              continue

          except Exception as e:
              send_to_admin(f"❌ HATA: {str(e)[:100]}")
              exit(1)

          if not price:
              send_to_admin(f"⚠️ Fiyat alinamadi: {URL}")
              exit(0)

          db = get_data()
          now = get_time()
          
          # ILK CALISTIRMA
          if db["price"] == 0:
              msg = "🛍️ <b>YENİ ÜRÜN TAKİBİ</b>\n"
              msg += "━━━━━━━━━━━━━━━━━━━━\n\n"
              msg += f"📦 <b>{product_name}</b>\n\n"
              msg += f"💰 Guncel Fiyat: <b>{format_price(price)}</b>\n\n"
              msg += f"🔗 <a href='{URL}'>Urunu Gor</a>\n\n"
              msg += "━━━━━━━━━━━━━━━━━━━━\n"
              msg += "ℹ️ Fiyat degisirse gruba bildirim gelecek\n"
              msg += "⏰ 6 saatte bir durum raporu paylasilacak"
              
              send_to_group(msg)
              db["price"] = price
              db["first_price"] = price
              db["lowest_price"] = price
              db["highest_price"] = price
              db["check_count"] = 1
              db["start_date"] = now
              save_data(db)
              exit(0)

          old_price = db["price"]
          
          # FIYAT DEGISTI
          if old_price != price:
              diff = old_price - price
              percent = (diff / old_price) * 100
              
              if price < db["lowest_price"]:
                  db["lowest_price"] = price
              if price > db["highest_price"]:
                  db["highest_price"] = price
              
              if price < old_price:
                  msg = "🔥 <b>FİYAT DÜŞTÜ!</b> 🔥\n"
                  msg += "━━━━━━━━━━━━━━━━━━━━\n\n"
                  msg += f"📦 <b>{product_name}</b>\n\n"
                  msg += f"💸 Eski Fiyat: <s>{format_price(old_price)}</s>\n"
                  msg += f"💰 Yeni Fiyat: <b>{format_price(price)}</b>\n\n"
                  msg += f"📉 Dusus: <b>{format_price(abs(diff))}</b> (-%{abs(percent):.1f})\n\n"
                  msg += "━━━━━━━━━━━━━━━━━━━━\n"
                  msg += f"📊 Takipteki en dusuk: {format_price(db['lowest_price'])}\n"
                  msg += f"📊 Takipteki en yuksek: {format_price(db['highest_price'])}\n\n"
                  msg += f"🔗 <a href='{URL}'>SATIN AL</a>\n\n"
                  msg += f"⏰ {now}"
              else:
                  msg = "📈 <b>FİYAT ARTTI</b>\n"
                  msg += "━━━━━━━━━━━━━━━━━━━━\n\n"
                  msg += f"📦 <b>{product_name}</b>\n\n"
                  msg += f"💰 Eski Fiyat: {format_price(old_price)}\n"
                  msg += f"💸 Yeni Fiyat: <b>{format_price(price)}</b>\n\n"
                  msg += f"📈 Artis: <b>{format_price(abs(diff))}</b> (+%{abs(percent):.1f})\n\n"
                  msg += "━━━━━━━━━━━━━━━━━━━━\n"
                  msg += f"📊 Takipteki en dusuk: {format_price(db['lowest_price'])}\n"
                  msg += f"📊 Takipteki en yuksek: {format_price(db['highest_price'])}\n\n"
                  msg += f"🔗 <a href='{URL}'>Urunu Gor</a>\n\n"
                  msg += f"⏰ {now}"
              
              send_to_group(msg)
              db["price"] = price
              db["check_count"] = 0
              save_data(db)
              exit(0)

          # 6 SAAT RAPOR
          db["check_count"] += 1
          if db["check_count"] >= 12:
              first_p = db.get("first_price", price)
              total_diff = first_p - price
              
              msg = "⏰ <b>6 SAATLİK RAPOR</b>\n"
              msg += "━━━━━━━━━━━━━━━━━━━━\n\n"
              msg += f"📦 <b>{product_name}</b>\n\n"
              msg += f"💰 Guncel Fiyat: <b>{format_price(price)}</b>\n\n"
              
              if total_diff > 0:
                  msg += f"📉 Baslangictan bu yana: <b>{format_price(abs(total_diff))}</b> dusus\n"
              elif total_diff < 0:
                  msg += f"📈 Baslangictan bu yana: <b>{format_price(abs(total_diff))}</b> artis\n"
              else:
                  msg += "➖ Baslangictan bu yana fiyat degismedi\n"
              
              msg += "\n━━━━━━━━━━━━━━━━━━━━\n"
              msg += f"📊 En dusuk: {format_price(db['lowest_price'])}\n"
              msg += f"📊 En yuksek: {format_price(db['highest_price'])}\n"
              msg += f"📅 Takip baslangici: {db.get('start_date', '-')}\n\n"
              msg += f"🔗 <a href='{URL}'>Urunu Gor</a>\n\n"
              msg += "✅ Takip aktif"
              
              send_to_group(msg)
              db["check_count"] = 0
          
          save_data(db)
          PYEND

      - name: Commit files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          touch data.json
          git add -A
          git diff --staged --quiet || git commit -m "Update"
          git push
